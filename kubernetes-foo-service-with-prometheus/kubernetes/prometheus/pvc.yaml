apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: prometheus
  namespace: monitoring
spec:
  storageClassName: local-storage
  volumeName: prometheus
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 1Gi
